### Description
---
The ability to manage `multiple` tasks or processes `simultaneously` allowing them to process `independently` and `overlap` in execution. This is achieved through `Context Switching`

### Key Features
---
- Requires [[Synchronization]]